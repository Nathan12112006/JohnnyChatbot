name: Combined-query active-barbecuing release

on:
  push:
    branches:
      - barbecue-dataset-build-20260902
    paths:
      - "fast_barbecue_release_v3.py"
      - ".github/workflows/fast-barbecue-release-v3.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: combined-active-barbecuing-release-v3
  cancel-in-progress: true

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      PYTHONUNBUFFERED: "1"
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check out build branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            Pillow==11.1.0 \
            ImageHash==4.3.1 \
            requests==2.32.3 \
            tqdm==4.67.1

      - name: Build and validate exactly 1,000 images
        run: |
          python fast_barbecue_release_v3.py \
            --target 1000 \
            --output fast_barbecue_output_v3 \
            --per-query 300 \
            --search-limit 3000 \
            --metadata-limit 2800 \
            --download-limit 2200 \
            --workers 24 \
            --pause 0.8 \
            --max-side 640 \
            --min-side 256 \
            --phash-limit 5 \
            --dhash-limit 4 \
            --creator-cap 40 \
            --query-cap 220

      - name: Publish stable public files
        run: |
          TAG="active-barbecuing-dataset-1000-v3"
          gh release delete "$TAG" --yes --cleanup-tag || true
          gh release create "$TAG" \
            fast_barbecue_output_v3/active_barbecuing_dataset_1000.zip \
            fast_barbecue_output_v3/active_barbecuing_preview_100.jpg \
            fast_barbecue_output_v3/build_summary.json \
            --target "$GITHUB_SHA" \
            --title "Active Barbecuing Image Dataset — 1,000 images" \
            --notes "Wikimedia Commons candidate collection. The ZIP passed archive-integrity, exact image-count, image-decoding, and SHA-256 duplicate checks. Automated relevance filtering should be manually reviewed before training."

      - name: Upload Actions artifact fallback
        uses: actions/upload-artifact@v4
        with:
          name: active-barbecuing-dataset-1000-v3
          path: |
            fast_barbecue_output_v3/active_barbecuing_dataset_1000.zip
            fast_barbecue_output_v3/active_barbecuing_preview_100.jpg
            fast_barbecue_output_v3/build_summary.json
            fast_barbecue_output_v3/selected_metadata.jsonl
          if-no-files-found: error
          compression-level: 0
          retention-days: 14
